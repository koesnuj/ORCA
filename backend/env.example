# Database
DATABASE_URL="postgresql://username:password@localhost:5432/tms_db"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001
NODE_ENV="development"

# CORS
FRONTEND_URL="http://localhost:5173"

# Optional: additional allowed origins (comma separated)
# Example: CORS_ALLOWED_ORIGINS="https://your-vercel-app.vercel.app,https://staging.example.com"
CORS_ALLOWED_ORIGINS=""

# Optional: allow any *.vercel.app origin (preview deployments)
# Recommended: keep false unless you really need previews.
CORS_ALLOW_VERCEL_PREVIEW="false"

